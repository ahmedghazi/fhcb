import { BlockquoteUI, HrUI } from "@/app/sanity-api/types/sanity.types";
import { _localizeField } from "@/app/sanity-api/utils";
import React from "react";

const ModuleHrUI = () => {
  return (
    <section className='module module--hr-ui'>
      <div className='container-fluid'>
        <hr />
      </div>
    </section>
  );
};

export default ModuleHrUI;
